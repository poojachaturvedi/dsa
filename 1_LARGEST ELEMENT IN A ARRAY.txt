USIG SORTING
USING THE LOOP
{
      
      int max=INT_MIN;
      for(int i=0;i<n;i++){
          if(arr[i]>max)
          max=arr[i];
          
      }   
      return max;
      }

LOOP is the optimized approach
